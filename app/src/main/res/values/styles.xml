<resources>

    <!-- Base application theme. -->
    <style name="CalendarDaysCustomText" parent="android:TextAppearance.DeviceDefault.Medium">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="CalendarMonthCustomText" parent="android:TextAppearance.DeviceDefault.Large">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
